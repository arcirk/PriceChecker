#ifndef SYNCH_BASE_DOCUMENTS_HPP
#define SYNCH_BASE_DOCUMENTS_HPP

#include <QObject>
#include "wsSettings.hpp"

//class SynchDocumentsBase : public QObject{
//    Q_OBJECT
//public:
//    explicit SynchDocumentsBase(arcirk::Settings *sett, QObject *parent = nullptr);

//    void synchronize();
//    void setComparisonOfDocuments(const nlohmann::json& resp);
//private:
//    bool synchronizeBaseDocuments(nlohmann::json& sendToSrvDocuments);
//signals:
//    void endSynchronize(bool result, const nlohmann::json& send_srv);
//    void startSynchronize();
//};

#endif // SYNCH_BASE_DOCUMENTS_HPP
